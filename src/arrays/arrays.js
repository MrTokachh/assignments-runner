export function mapTo(a) {
  // return a.map((x,  index) =>  index)
  // return a.map(item => item.name)
  // TODO:
  throw 'Not implemented';
}

export function mapToProfile() {
  // TODO:
  throw 'Not implemented';
}

export function filterBy(a) {
  // return a.filter(n => n>=5)
  // return a.filter(n => n.name)
  // return a.filter(n => n.age <=10)

  // TODO:
  throw 'Not implemented';
}

export function reduceTo(arr) {
  // return arr.reduce((a, b) => a + b, -10)
  // return arr.reduce((a, b) => a + b.total, 0)

  // let array = [arr.reduce((a, b) => a + b.total, 0),  arr.reduce((a, b) => a + b.difference, 0)]
  // return array

  // TODO:
  throw 'Not implemented';
}

export function sort(a) {
  // return a.sort((b, c) => b - c)
  // return a.sort((b,c) => b.age - c.age || b.total - c.total)
  // return a.sort((b,c) => b.total - c.total || c.age - b.age)

  throw 'Not implemented';
}

export function complex(a) {
  // return a.filter(n => n.age>4).map(item =>item.total)
  // return a.filter(n => n.total<10).reduce((a, b) => a + b.total, 0)
  // return a.filter(n => n.age>=4).map(item => item.total).sort((b, c) =>  c - b)
  // TODO:
  throw 'Not implemented';
}
